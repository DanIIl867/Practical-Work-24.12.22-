let a = 1, b = 1;

alert(++a); // 2, префіксна форма повертає нове значення
alert(b++); // 1, постфіксна форма повертає старе значення
alert(a); // 2, значення збільшено один раз
alert(b); // 2, значення збільшено один раз